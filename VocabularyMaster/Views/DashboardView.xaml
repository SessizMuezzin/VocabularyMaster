<UserControl x:Class="VocabularyMaster.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="📊 Dashboard" FontSize="32" FontWeight="Bold"/>
                    <TextBlock Text="Öğrenme İlerlemeniz" FontSize="16" Foreground="#666" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="🔄 Yenile" Command="{Binding RefreshCommand}"
                        Padding="15,8" Background="#2196F3" Foreground="White" FontSize="14" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"/>
            </Grid>

            <!-- Stats Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Toplam Kelime -->
                <Border Grid.Column="0" Background="#4CAF50" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="📚" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Toplam Kelime" FontSize="14" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                        <TextBlock Text="{Binding TotalWords}" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Bugün Eklenen -->
                <Border Grid.Column="2" Background="#2196F3" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="📅" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Bugün Eklenen" FontSize="14" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                        <TextBlock Text="{Binding TodayAdded}" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Bu Hafta -->
                <Border Grid.Column="4" Background="#FF9800" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Bu Hafta" FontSize="14" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                        <TextBlock Text="{Binding WeekAdded}" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Favoriler -->
                <Border Grid.Column="6" Background="#E91E63" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="⭐" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Favoriler" FontSize="14" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                        <TextBlock Text="{Binding FavoriteCount}" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Activity Stats -->
            <Grid Grid.Row="2" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Bugünkü Test -->
                <Border Grid.Column="0" Background="#9C27B0" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="🎯" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="Bugün Test Edilen" FontSize="14" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                        <TextBlock Text="{Binding TodayReviews}" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Streak -->
                <Border Grid.Column="2" Background="#FF5722" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="🔥" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="Günlük Seri" FontSize="14" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                            <TextBlock Text="{Binding CurrentStreak}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text=" gün" FontSize="18" Foreground="White" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Son Eklenen Kelimeler -->
            <StackPanel Grid.Row="3">
                <TextBlock Text="🆕 Son Eklenen Kelimeler" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

                <Border Background="White" BorderBrush="#e0e0e0" BorderThickness="1" CornerRadius="5">
                    <DataGrid ItemsSource="{Binding RecentWords}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserResizeRows="False"
                  RowHeight="40"
                  BorderThickness="0"
                  SelectionMode="Single"
                  Background="White"
                  AlternatingRowBackground="#f9f9f9">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="" Width="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#4CAF50" CornerRadius="3" Padding="6,3" HorizontalAlignment="Center">
                                            <TextBlock Text="NEW" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="⭐" Width="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding IsFavorite, Converter={StaticResource FavoriteIconConverter}}" 
                                       FontSize="18" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="İngilizce" Binding="{Binding English}" Width="*" FontSize="14" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Türkçe" Binding="{Binding Turkish}" Width="*" FontSize="14"/>
                            <DataGridTextColumn Header="Kategori" Binding="{Binding Category}" Width="120" FontSize="14"/>
                            <DataGridTextColumn Header="Seviye" Binding="{Binding DifficultyLevel}" Width="80" FontSize="14"/>
                            <DataGridTextColumn Header="Tekrar" Binding="{Binding ReviewCount}" Width="80" FontSize="14"/>
                            <DataGridTextColumn Header="Başarı %" Binding="{Binding SuccessRate, StringFormat=N1}" Width="90" FontSize="14"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>